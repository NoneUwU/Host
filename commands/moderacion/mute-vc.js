const { Client } = require("discord.js");
const Discord = require('discord.js')

module.exports  = {
    name: "mute-vc",
    aliases : ['mvc'],
    description: "Mutear de un canal de voz a un usuario.",
    run : async(client, message, args) => {

    }
}