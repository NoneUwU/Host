const { Client } = require("discord.js");
const Discord = require('discord.js')

module.exports  = {
    name: "ticket",
    aliases : ['t'],
    description: "Iniciar ticket de un usuario.",
    run : async(client, message, args) => {

    }
}